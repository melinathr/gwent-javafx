package model;

import enums.CardPlacement;

public class Weather extends CardRow {
    public Weather(CardPlacement cardPlacement, double horizentalPosition, double verticalPosition) {
        super(cardPlacement, horizentalPosition, verticalPosition);
    }
}
